  <div class="cocktail_container" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.3)), url('<%= cl_image_path(@cocktail.photo.key, crop: :fill) %>')">
       <!--  <%= link_to image_tag('cocktail_white', class: "home_button"), cocktails_path %> -->
  <div class="container_new">
    <div class="wrapper">
      <p class="headline_cocktail">
        <strong><%= @cocktail.name %></strong>
      </p>

      <div class="ingredients_with_delete">
        <div class="ingredients">
          <% @cocktail.doses.each do |dose| %>
          <p class="ingredient" data-dose-id="<%= dose.id%>" id="dose-<%= dose.id%>" ><%= dose.description %> <%= dose.ingredient.name  %>
            <br>
            <% end %>
          </p>
        </div>
        <div class="x">
          <% @cocktail.doses.each do |dose| %>
          <%= link_to image_tag('bin2', size: "11x11", class: "delete_image_ingredients"), dose_path(dose), method: :delete %>
          <br>
          <% end %>
        </div>
      </div>

      <div class="links_cocktail_new">
        <%= link_to image_tag('plus', class: "ingredient_link"), new_cocktail_dose_path(@cocktail)%>
<!--  <%= link_to 'Delete Cocktail', cocktail_path(@cocktail), method: :delete, data: {confirm: 'are you sure?'}, class: "delete_cocktails_link" %>
 -->   </div>
    </div>
    </div>
  </div>

<script>
  // const headline = document.querySelector('.headline_cocktail')
    // window.onload = function() {
        // setTimeout(function () {
            // location.reload()
        // }, 2000);
     // };
</script>

